# Dependencies
/node_modules

# Environment variables
.env
*.env
.env.*

# SQLite database file
# We are committing database.sqlite for the initial Render setup,
# so we will NOT add it to .gitignore right now.
# If you later manage it differently, you might add it here.
# database.sqlite

# OS-generated files
.DS_Store
Thumbs.db

# Log files
*.log
logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Firebase specific files (if you are not using Firebase for this deployment)
# .firebaserc
# firebase.json
# functions/node_modules - if you have a separate package.json in functions